{
  "private": true,
  "description": "Dev tooling for ClaudeMix (commit hooks only — ClaudeMix itself is pure bash)",
  "scripts": {
    "prepare": "husky",
    "lint": "bash -n bin/claudemix && for f in lib/*.sh; do bash -n \"$f\" && echo \"$f OK\"; done",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.2",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
